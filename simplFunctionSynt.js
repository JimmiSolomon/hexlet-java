const sum = (a, b) => a + b

//пример
//Функция которая принимает не пустую строку и приводит первую букву строки к верхнему регистру
const capitalize = (text) => `${text[0].toUpperCase()}${text.slice(1)}`;